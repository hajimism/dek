/** The slide rail's width in px: the page renders it, the player resizes it within these bounds. */
export const RAIL_WIDTH_DEFAULT = 188;
export const RAIL_WIDTH_MIN = 120;
export const RAIL_WIDTH_MAX = 360;
