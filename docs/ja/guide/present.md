# 発表する

作業中は開発サーバがブラウザをファイルと同期させます。会場では HTML ファイルが 1 つあれば足ります。このページではその両方に加えて、プレゼンタービュー、別デバイスからのリモート操作、PDF 書き出しを扱います。

## 開発サーバ

デッキの中で `dek` を叩けばそのデッキを、プロジェクト直下で叩けば全デッキの一覧を配信します。

```bash
dek
dek --visual
```

保存のたびに次が起きます。

- **`script.md`** — 新しい見出しに骨格が生成され、孤児になった HTML がブラウザに警告表示され、プレゼンターノートが更新されます。デッキに `voice/` があれば、変わった文だけが再合成されます。
- **`slides/*.html`** — 変更された枚だけがその場で差し替わり、lint が走り、診断が端末とブラウザのオーバーレイの両方に出ます。
- **`theme.css`** — 全スライドが再描画され、クラス語彙が再解決されます。
- **`slides/*.css`** — スタイルがその場で当て直され、lint が走ります。
- **`slides/*.ts`** — スライドスクリプトは読み込み時に一度だけ登録されるので、ページが再読み込みされ、lint が走ります。
- **`voice/voice.toml`** — キャッシュ済みのクリップからデッキのタイミングを組み直します。
- `--visual` を付けて Playwright があれば、保存した枚のはみ出しとコントラストも測ります。

プロジェクトごとにサーバは 1 つです。2 つ目の `dek` は `.dek/server.json` にある 1 つ目のロックを見つけ、その URL を案内します。

## プレゼンタービュー

`/presenter` を開くか、プレイヤーで `p` を押します。いまのスライド、次のビートかスライドのプレビュー、いまのセクションの台本が並び、いまのビートがハイライトされます。上端の進捗バーはデッキ全体の位置を示します。下端には枚数、いまのセクションの予算、そして最初に送ってからの経過時間。経過時間はトーク全体の予算の 80% を超えると黄色、100% を超えると赤になります。もう一度 `p` を押すと、同じウィンドウで観客ビューに戻ります。

接続しているすべてのウィンドウが WebSocket でプレゼンターに追従します。2 台目のモニタでも、別のノート PC でも、スマホでも。

```bash
dek goto architecture
dek current
```

サーバが立っている間、端末からブラウザを動かし、いま何の枚が出ているかを尋ねられます。エージェントも同じ 2 つのコマンドを使います。

## キー操作

| キー | 動作 |
| --- | --- |
| `→` `PageDown` | 次のビート。使い切ったら次のスライド |
| `←` | 前のビート。使い切ったら前のスライド |
| `p` | プレゼンタービューの切り替え |
| `s` | スライド一覧の表示切り替え |
| `↑` `↓` | スライド一覧の中を移動 |
| `Space` | `dek rehearse` 中の再生 / 停止 |

## 1 ファイル

```bash
dek build
```

`decks/<deck>/dist/<deck>.html` ができます。全スライド、テーマ、data URI 化した画像、プレイヤーのランタイムを minify して 1 ファイルにしたものです。ブラウザで開いて発表します。`--root-dist` を付けると、全デッキのビルドがプロジェクト直下の `dist/` に集まります。

build がやることは 4 つです。各 `<section class="slide">` を抜き出して `data-slug` を付ける、テーマを minify する、`assets/` を data URI にする、ランタイムを埋め込む。外部の HTML minifier は使わないので、入力の書き方がどうであれ出力は一つの形になります。HTML のないセクションがあると build は失敗します。先に開発サーバか `dek sync` を走らせてください。

`?presenter` を付けて開くか `p` を押すとプレゼンタービューになります。同じファイルを別ウィンドウで開けば、`BroadcastChannel` で最初のウィンドウに追従します。観客ビューをプロジェクタに、プレゼンタービューを手元に。サーバもネットワークも要りません。観客ビューには左にスライド一覧があり、サムネイルをクリックすればその枚へ飛べます。`s` で隠し、端をドラッグして幅を変えられます。

## 別のデバイスから

会場で開発サーバを使うのは、スマホをリモコンにするなど、別のデバイスからデッキを操作したいときだけです。

```bash
dek --remote
dek --remote --password s3cret
```

LAN に公開します。プレゼンタービュー、`goto`、`current` は HTTP Basic 認証の後ろにあり、観客ビューは開いています。`--password` を省くと dek がパスワードを生成し、URL とともに表示します。

USB メモリの 1 ファイルは、必ず動くフォールバックです。それ以外はすべて任意です。

## PDF

```bash
dek pdf
```

全スライドを最終ビートの状態で `dist/<deck>.pdf` に描き出します。Playwright が必要です。

## 次

ナレーションを付けて動画にする → [声と動画](./voice)。ライブだけで足りるなら [AI エージェントと作る](./ai)へ。
